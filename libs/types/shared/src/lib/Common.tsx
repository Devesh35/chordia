export type NextImage = {
  src: string;
  alt: string;
  width: number;
  height: number;
};

export type ReactChildren = {
  children: React.ReactNode;
};

export type ClassName = {
  className: string;
};
