'use client';

import { FormFormSection } from '@li/design/elements';
import { DialForm } from 'modules/blaunk/config/src/lib/options/Dial';

export const AddDial = () => {
  return <FormFormSection section={DialForm} />;
};
